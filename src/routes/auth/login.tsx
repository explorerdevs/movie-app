import { AuthFormTemplate, LoginForm } from '@src/components';

const LoginRoute = () => {
  return (
    <AuthFormTemplate>
      <LoginForm />
    </AuthFormTemplate>
  );
};

export { LoginRoute };
