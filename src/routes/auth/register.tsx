import { AuthFormTemplate, RegisterForm } from '@src/components';

const RegisterRoute = () => {
  return (
    <AuthFormTemplate>
      <RegisterForm />
    </AuthFormTemplate>
  );
};

export { RegisterRoute };
