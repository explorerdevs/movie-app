import { AuthFormTemplate, RegisterForm } from '@/components';

const RegisterRoute = () => {
  return (
    <AuthFormTemplate>
      <RegisterForm />
    </AuthFormTemplate>
  );
};

export { RegisterRoute };
